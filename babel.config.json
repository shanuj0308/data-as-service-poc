//The provided JSON snippet appears to be a Babel configuration file.
// Babel is a JavaScript compiler that allows you to use the latest JavaScript features
// while ensuring compatibility with older environments.
// The configuration file you provided specifies a preset, which is a set of plugins

// used to support particular language features.

{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": {
          "node": "current"
        }
      }
    ],
    [
      "@babel/preset-react",
      // ReferenceError: React is not defined
      {
        "runtime": "automatic"
      }
    ],
    [
      "babel-preset-vite",
      {
        "env": true,
        "glob": false
      }
    ],
    "@babel/preset-typescript"
  ]
}
